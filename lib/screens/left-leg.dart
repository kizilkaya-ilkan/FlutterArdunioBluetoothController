import 'package:flutter/material.dart';

class leftLeg extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hello World'),
      color: Colors.blue,
    );
  }
}
